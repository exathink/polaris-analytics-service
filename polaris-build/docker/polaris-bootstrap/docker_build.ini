[default]
name = polaris-base
description = This is the base image for all images in polaris.
    It contains basic python utilities as well as C shared libraries needed to build
    packages used by the rest of the system. Avoid adding anything that is not absolutely
    needed system wide into this image.


[polaris-base]
repository = polaris-base
build_context = ../..
